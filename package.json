{
  "name": "market-credo",
  "version": "1.0.0",
  "private": true,
  "description": "Market Credo — static site build guards (NAP lint, sitemap).",
  "scripts": {
    "build": "node scripts/check-nap.js"
  }
}
